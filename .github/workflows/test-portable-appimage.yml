name: Test Harbor portable AppImage

on:
  workflow_dispatch:
    inputs:
      source_ref:
        description: Harbor commit or ref to build.
        required: true
        type: string

permissions:
  contents: read

jobs:
  build:
    name: Build experimental portable AppImage on Arch
    runs-on: ubuntu-24.04
    # The experimental sharun-based Tauri bundler currently requires Arch.
    # Keep this isolated from release workflows until upstream graduates it.
    container: archlinux:base-devel
    timeout-minutes: 150
    steps:
      - name: Checkout packaging workflow
        uses: actions/checkout@v7.0.0

      - name: Install Arch build dependencies
        run: |
          set -euo pipefail
          pacman -Syu --noconfirm --needed \
            alsa-lib base-devel ca-certificates curl ffmpeg git gstreamer \
            gst-libav gst-plugins-bad gst-plugins-base gst-plugins-good \
            gtk3 libayatana-appindicator librsvg mpv nodejs npm openssl \
            patchelf pkgconf rust webkit2gtk-4.1 yt-dlp
          npm install --global pnpm@11.9.0

      - name: Checkout Harbor source
        id: source
        env:
          INPUT_SOURCE_REF: ${{ inputs.source_ref }}
        run: |
          set -euo pipefail
          git init harbor
          git -C harbor remote add origin https://github.com/harborstremio/harbor.git
          git -C harbor fetch --depth 1 origin "${INPUT_SOURCE_REF}"
          git -C harbor checkout --detach FETCH_HEAD
          echo "source_ref=$(git -C harbor rev-parse HEAD)" >> "${GITHUB_OUTPUT}"

      - name: Prepare Harbor workspace
        working-directory: harbor
        run: |
          set -euo pipefail
          node <<'EOF'
          const fs = require('fs');
          const workspacePath = 'pnpm-workspace.yaml';
          let workspace = fs.readFileSync(workspacePath, 'utf8');
          const setAllowBuild = (name, value) => {
            const entry = new RegExp(`^(\\s*)${name}:.*$`, 'm');
            if (entry.test(workspace)) {
              workspace = workspace.replace(entry, `$1${name}: ${value}`);
              return;
            }
            if (/^allowBuilds:\s*$/m.test(workspace)) {
              workspace = workspace.replace(/^allowBuilds:\s*$/m, `allowBuilds:\n  ${name}: ${value}`);
              return;
            }
            workspace += `${workspace.endsWith('\n') || !workspace ? '' : '\n'}allowBuilds:\n  ${name}: ${value}\n`;
          };
          setAllowBuild('esbuild', 'true');
          setAllowBuild('protobufjs', 'false');
          fs.writeFileSync(workspacePath, workspace);
          EOF
          pnpm install --lockfile-only --no-frozen-lockfile
          pnpm install --frozen-lockfile
          if node -e "process.exit(require('./package.json').scripts?.setup ? 0 : 1)"; then
            pnpm run setup
          else
            if node -e "process.exit(require('./package.json').scripts?.['setup:fonts'] ? 0 : 1)"; then pnpm run setup:fonts; fi
            mkdir -p src-tauri/binaries
            ln -sf /usr/bin/ffmpeg src-tauri/binaries/ffmpeg-x86_64-unknown-linux-gnu
            ln -sf /usr/bin/ffprobe src-tauri/binaries/ffprobe-x86_64-unknown-linux-gnu
            ln -sf /usr/bin/yt-dlp src-tauri/binaries/yt-dlp-x86_64-unknown-linux-gnu
          fi

      - name: Install experimental Tauri bundler
        run: |
          set -euo pipefail
          cargo install tauri-cli \
            --git https://github.com/tauri-apps/tauri \
            --rev 162626969b382d52faebdf6c7264460bd9a47d1f \
            --locked \
            --root "${RUNNER_TEMP}/tauri-cli"
          echo "${RUNNER_TEMP}/tauri-cli/bin" >> "${GITHUB_PATH}"

      - name: Disable updater artifacts
        working-directory: harbor
        run: |
          node -e '
          const fs = require("fs");
          const path = "src-tauri/tauri.conf.json";
          const config = JSON.parse(fs.readFileSync(path, "utf8"));
          config.bundle.createUpdaterArtifacts = false;
          fs.writeFileSync(path, `${JSON.stringify(config, null, 2)}\n`);
          '

      - name: Build portable AppImage
        working-directory: harbor
        env:
          TAURI_BUNDLER_NEW_APPIMAGE_FORMAT: "true"
        run: timeout --signal=TERM 90m cargo tauri build --target x86_64-unknown-linux-gnu --bundles appimage

      - name: Collect AppImage artifact
        run: |
          set -euo pipefail
          appimage="$(find harbor/src-tauri/target/x86_64-unknown-linux-gnu/release/bundle/appimage -type f -name '*.AppImage' -print -quit)"
          test -n "${appimage}"
          mkdir dist
          cp "${appimage}" dist/
          sha256sum dist/*.AppImage > dist/SHA256SUMS

      - name: Upload experimental artifact
        uses: actions/upload-artifact@v7.0.0
        with:
          name: portable-appimage-${{ steps.source.outputs.source_ref }}
          path: dist/
          if-no-files-found: error
